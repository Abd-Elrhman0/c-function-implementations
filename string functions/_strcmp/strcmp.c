#include "main.h"

/**
 * _strcmp - compares two strings and returns an integer indicating
 * which one is greater.
 *
 * @str: One of the strings to be compared.
 * @str1: One of the strings to be compared.
 *
 * Return: An int value which is positive if the first string is
 * greatier, negative if the second string is greater and 0 if the
 * two strings are equal.
 */

int _strcmp(char str[], char str1[])
{
	return (str - str1);
}
