#include "main.h"

/**
 * main - test the code.
 *
 * Return: 0.
 */

int main(void)
{
	char str[] = "abd.elrhman";
	char str1[20];

	_strcpy(str1, str);
	printf("%s\n", str);
	printf("%s\n", str1);
	return (0);
}
