#include "main.h"

/**
 * main - test the code.
 *
 * Return: 0.
 */

int main(void)
{
	char str[20] = "abd.elrhman hosam";

	printf("%d\n", _strlen(str));
	printf("%lu\n", sizeof(str));
	return (0);
}
